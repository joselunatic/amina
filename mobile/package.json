{
  "name": "esoweb-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper for esoweb using Capacitor.",
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync android",
    "open": "cap open android",
    "run:android": "cap run android",
    "doctor": "cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
